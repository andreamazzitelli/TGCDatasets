# Dataset images and generated Create ML artifacts are not tracked here —
# see README.md "Why images aren't committed here".
data/tcg_identifier
data/tcg_identifier_augmented
data/grading_status
data/index
*.mlmodel
*.mlmodelc/

# Keep the manifest (provenance/licensing tracking) tracked.
!data/manifest.csv

.DS_Store
__pycache__/

# Local virtualenv for the download scripts
.venv/

# Swift package build products / run logs (tools/TrainClassifier)
.build/
full_run.log
